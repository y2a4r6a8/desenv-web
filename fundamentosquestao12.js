function unirElementos(lista) {
    
    return lista.join("#");
  }
  const elementos = ["maçã", "banana", "laranja", "uva"];
  const resultado = unirElementos(elementos);
  console.log(resultado);
  